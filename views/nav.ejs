<nav class="navbar navbar-expand-lg  navbar-dark bg-dark">
    <a class="navbar-brand font-weight-bolder" href="/">Blog</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link text-white" href="/">Home</a>
        </li>
        <li class="nav-item ">
          <a class="nav-link text-white" href="/article/new">New</a>
        </li>
        <% if (user.status === 'loggedIn') { %>
          <a class="nav-link text-white" href="/article/posts">Posts</a>
        <% } %>
      </ul>
    </div>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
          <% if (user.name) { %>
            <li class="nav-item">
              <a class="nav-link text-white" href="/article/posts"><%= user.name %></a>
            </li>
          <% } %>
          <% if (user.status === 'loggedIn') { %>
            <li class="nav-item">
              <form action="/account/logout?_method=DELETE" method="POST">
                <button class="btn btn-outline-danger my-2 my-sm-0 ml-2" type="submit">logout</button>
              </form>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a href="/account/login"><button class="btn btn-outline-success my-2 my-sm-0 " type="submit">login</button></a>
            </li>
          <% } %>           
      </ul>
  </div>
  </nav>